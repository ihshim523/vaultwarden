[build]
builder = "DOCKERFILE"

[deploy]
startCommand = ""
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[deploy.volumeMounts]]
name = "vaultwarden-data"
mountPath = "/data"
